/*Task
Coding in function , function accept 1 parameters:, try to use the conditional statement if...else, 
if value is false (val==false or it can convert to false), should return a string "false", 
if not, return a string "true".trueOrFalsevalval
*/

function trueOrFalse(val){
  if (val)    return 'true';             
  else     return 'false';
}
